<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="AIO taller mecanico en montevideo, Estacionamiento 24 horas y mas">
    <meta name="keywords" content="Taller mecanico, taller mecanico montevideo, taller mecanico cerca de mi, estacionamiento 24 horas, venta de neumaticos">
    <title>All In One - SIGAE</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('ico/logo-positivo.ico') }}">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @font-face {
            font-family: 'Teachers';
            src: url("{{ asset('font/Teachers-VariableFont_wght.ttf') }}");
        }
        
        body {
            font-family: 'Teachers', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Header incluido via Twig -->
    {% include 'partials/headerGerente.html.twig' %}

    <main class="container mx-auto px-4 py-12 max-w-6xl">
        <!-- Mensajes de Error -->
        {% if response.errors is defined and response.errors is not empty %}
            <div id="error-container" class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded-md">
                <ul class="list-disc pl-4">
                    {% for error in response.errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <!-- Grid de Servicios -->
        <div class="grid gap-6">
            <!-- Header Taller -->
            <div class="bg-neutral-400 rounded-2xl shadow-lg mb-8 p-8">
                <h1 class="text-white text-4xl font-bold text-center">Taller de Alineación y Balanceo</h1>
            </div>
            <!-- Disponibilidad de Taller -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Disponibilidad</h2>
                    <p class="text-gray-600 mb-6">Consulte los horarios disponibles del taller por lapsos</p>
                    <a href="{{ path('showWorkshopAvailability') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Consultar
                        </button>
                    </a>
                </div>
            </div>

            <!-- Servicios Disponibles de Taller -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Servicios disponibles</h2>
                    <p class="text-gray-600 mb-6">Explore nuestra gama de servicios mecánicos y de diagnóstico disponibles</p>
                    <a href="{{ path('showWorkshopServices') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Ver Servicios
                        </button>
                    </a>
                </div>
            </div>

            <!-- Header Parking -->
            <div class="bg-neutral-400 rounded-2xl shadow-lg mb-8 p-8">
                <h1 class="text-white text-4xl font-bold text-center">AIO Parking</h1>
            </div>
            <!-- Disponibilidad de Parking -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Disponibilidad de plazas</h2>
                    <p class="text-gray-600 mb-6">Consulte las plazas del estacionamiento disponibles en tiempo real</p>
                    <a href="{{ path('showParkingAvailability') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Consultar
                        </button>
                    </a>
                </div>
            </div>

            <!-- Header Ventas u Órdenes -->
            <div class="bg-neutral-400 rounded-2xl shadow-lg mb-8 p-8">
                <h1 class="text-white text-4xl font-bold text-center">Reportes del mes anterior</h1>
            </div>
            <!-- Informe de ventas de productos -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Ventas de productos</h2>
                    <p class="text-gray-600 mb-6">Consulte los productos más vendidos y más detalles</p>
                    <a href="{{ path('showProductSalesReport') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Ver reporte
                        </button>
                    </a>
                </div>
            </div>
            <!-- Informe de ventas de Parking -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Ventas de AIO Parking</h2>
                    <p class="text-gray-600 mb-6">Consulte las ventas de reservas del estacionamiento</p>
                    <a href="{{ path('showParkingSalesReport') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Ver reporte
                        </button>
                    </a>
                </div>
            </div>
            <!-- Informes de ventas de Taller -->
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200">
                <div class="p-8">
                    <h2 class="text-3xl font-bold text-gray-900 mb-4">Ventas de servicios del taller</h2>
                    <p class="text-gray-600 mb-6">Consulte las ventas de servicios realizados del taller de alineación y balanceo</p>
                    <a href="{{ path('showWorkshopServicesSalesReport') }}" class="block w-full">
                        <button 
                            class="w-full bg-red-600 hover:bg-red-700 text-white text-xl font-semibold py-4 px-6 rounded-xl transition-colors duration-300 flex items-center justify-center gap-2">
                            Ver reporte
                        </button>
                    </a>
                </div>
            </div>

        </div>
    </main>

    <!-- Footer incluido via Twig -->
    {% include 'partials/footer.html.twig' %}

    <!-- Script de traducción -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const archivoTraduccionHeader = "{{ asset('translate/header.json') }}";
            inicializarTraduccion(archivoTraduccionHeader, archivoTraduccionHome, 'es');
        });
    </script>
</body>
</html>