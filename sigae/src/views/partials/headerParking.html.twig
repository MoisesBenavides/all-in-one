<header class="w-screen">
    <div class="container mx-auto px-4 py-4">
        <div class="relative flex flex-col md:flex-row justify-between items-center">
            <!-- Hamburguesa (visible solo en móviles) -->
            <div class="absolute top-2 left-4 md:hidden">
                <button id="hamburger" class="text-black focus:outline-none">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                    </svg>
                </button>
            </div>

            <!-- Logo -->
            <a href="{{ path('aioParking') }}">
                <div class="w-40 h-auto mb-4 md:mb-0 mx-auto md:mx-0">
                    <img class="w-full h-auto" src="{{ asset('img/AIO Parking positivo.png') }}" alt="AIO Logo" />
                </div>
            </a>

            <!-- Navegación -->
            <nav class="hidden md:flex flex-wrap justify-center md:justify-end space-x-2 md:space-x-4">
                <a href="{{ path('home') }}" class="text-black text-xs sm:text-sm md:text-lg lg:text-xl font-bold">All In One</a>
                <a href="{{ path('aioParkingBookSimple') }}" class="text-black text-xs sm:text-sm md:text-lg lg:text-xl font-bold">Reservar</a>
                <a href="{{ path('aioParkingBookLongTerm') }}" class="text-black text-xs sm:text-sm md:text-lg lg:text-xl font-bold">Reservar a largo plazo</a>
                <a href="#" class="text-black text-xs sm:text-sm md:text-lg lg:text-xl font-bold">Mis Reservas</a>
                <a href="{{ path('faq') }}" class="text-black text-xs sm:text-sm md:text-lg lg:text-xl font-bold">FAQ</a>
            </nav>

            <!-- User icon y selector de idioma -->
            <div class="absolute top-2 right-4 md:static flex items-center space-x-4">
                <!-- Selector de idioma -->
                <div class="relative">
                    <button id="languageToggle" class="w-8 h-8 rounded-full hover:scale-110 transition-transform duration-300 focus:outline-none">
                        <img src="{{ asset('img/world.png') }}" alt="Language" class="w-full h-full object-cover">
                    </button>
                    
                    <!-- Menú desplegable de idiomas -->
                    <div id="languageMenu" class="hidden absolute right-0 mt-2 py-2 w-12 bg-white rounded-lg shadow-lg transform scale-95 transition-all duration-200 ease-in-out">
                        <button data-idioma="es" class="w-full px-2 py-1 hover:bg-gray-100 transition-colors duration-200">
                            <img src="{{ asset('img/es.png') }}" alt="Español" class="w-full h-5 object-cover">
                        </button>
                        <button data-idioma="it" class="w-full px-2 py-1 hover:bg-gray-100 transition-colors duration-200">
                            <img src="{{ asset('img/it.png') }}" alt="Italiano" class="w-full h-5 object-cover">
                        </button>
                        <button data-idioma="en" class="w-full px-2 py-1 hover:bg-gray-100 transition-colors duration-200">
                            <img src="{{ asset('img/en.png') }}" alt="English" class="w-full h-5 object-cover">
                        </button>
                    </div>
                </div>

                <!-- User icon -->
                <a href="{{ path('myAccount') }}" class="hover:scale-110 transition-transform duration-300">
                    <img class="w-10 h-10" src="{{ asset('img/user.png') }}" alt="User">
                </a>
            </div>
        </div>

        <!-- Menú móvil -->
        <nav id="mobileMenu" class="hidden md:hidden mt-4 space-y-2">
            <a href="{{ path('aioParkingBookSimple') }}" class="block text-black text-sm font-bold">Reservar</a>
            <a href="{{ path('aioParkingBookLongTerm') }}" class="block text-black text-sm font-bold">Reservar a largo plazo</a>
            <a href="#" class="block text-black text-sm font-bold">Mis Reservas</a>
            <a href="{{ path('faq') }}" class="block text-black text-sm font-bold">FAQ</a>
        </nav>
    </div>

    <script src="{{ asset('js/traduccion.js') }}"></script>
    <script>
        // Lógica del menú hamburguesa
        const hamburger = document.getElementById('hamburger');
        const mobileMenu = document.getElementById('mobileMenu');

        hamburger.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });

        // Lógica del selector de idioma
        const languageToggle = document.getElementById('languageToggle');
        const languageMenu = document.getElementById('languageMenu');

        languageToggle.addEventListener('click', () => {
            languageMenu.classList.toggle('hidden');
            // Añadir animación de escala
            if (!languageMenu.classList.contains('hidden')) {
                languageMenu.classList.add('scale-100');
                languageMenu.classList.remove('scale-95');
            } else {
                languageMenu.classList.add('scale-95');
                languageMenu.classList.remove('scale-100');
            }
        });

        // Cerrar el menú cuando se hace clic fuera
        document.addEventListener('click', (event) => {
            if (!languageToggle.contains(event.target) && !languageMenu.contains(event.target)) {
                languageMenu.classList.add('hidden');
                languageMenu.classList.add('scale-95');
                languageMenu.classList.remove('scale-100');
            }
        });

        // Lógica de traducción
        const archivoTraduccionHeader = "{{ asset('traductions/header.json') }}";

        document.addEventListener('DOMContentLoaded', () => {
            inicializarTraduccion(archivoTraduccionHeader, 'es');
        });

        languageMenu.querySelectorAll('button').forEach(button => {
            button.addEventListener('click', (e) => {
                const idioma = e.currentTarget.getAttribute('data-idioma');
                cambiarIdioma(idioma);
                languageMenu.classList.add('hidden');
            });
        });
    </script>
</header>