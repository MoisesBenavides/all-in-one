<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <!--SEO-->
    <meta name="description" content="AIO taller mecanico en montevideo, Estacionamiento 24 horas y mas">
    <meta name="keywords" content="Taller mecanico, taller mecanico montevideo, taller mecanico cerca de mi, estacionamiento 24 horas, venta de neumaticos">
    <meta name="author" content="All In One">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catálogo | AIO</title>
    <!--Es responsivo!!-->
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <link rel="icon" type="image/x-icon" href="{{ asset('ico/logo-positivo.ico') }}">
    <style>
        @font-face {
            font-family: 'Teachers';
            src: url("{{ asset('font/Teachers-VariableFont_wght.ttf') }}");
            font-weight: normal;
            font-style: normal;
        }
    
        body, input, select, button, option {
            font-family: 'Teachers', sans-serif;
        }

        .product {
            transition: opacity 0.3s ease;
        }

        .hidden {
            display: none;
        }
    </style>
</head>
        {% include 'partials/header.html.twig' %}


    <body class="bg-white">
        <div class="container mx-auto px-4 py-8">
            <div class="max-w-4xl mx-auto flex flex-col items-center gap-8">
                <div class="w-full bg-[#d4d2d2] rounded-md p-4">
                    <h1 class="text-center text-black text-2xl md:text-3xl font-semibold">Nuestros productos</h1>
                </div>
                <div class="w-full max-w-md">
                    <div class="relative">
                        <input type="text" id="buscar" placeholder="Buscar" class="w-full h-12 px-4 pr-10 rounded-md border border-black">
                        <img id="searchIcon" class="absolute right-3 top-1/2 transform -translate-y-1/2 w-6 h-6 cursor-pointer" src="{{ asset('svg/search.svg') }}" alt="Search">
                    </div>
                </div>
            </div>
            <div id="productosContainer" class="mt-8 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                <div class="product flex flex-col items-center">
                    <img class="w-full h-auto object-cover" src="{{ asset('img/Cubierta Pirelli1.webp') }}" alt="Cubierta Pirelli" />
                    <h2 class="mt-4 text-center text-[#161212] text-xl font-bold leading-tight">Cubierta Pirelli 175/70 R14 Scorpion Atr</h2>
                </div>
                <div class="product flex flex-col items-center">
                    <img class="w-full h-auto object-cover" src="{{ asset('img/Neumático Bridgestone1.webp') }}" alt="Neumático Bridgestone" />
                    <h2 class="mt-4 text-center text-[#161212] text-xl font-bold leading-tight">Neumático Bridgestone Ecopia EP150 P 185/55R16 83 V</h2>
                </div>
                <div class="product flex flex-col items-center">
                    <img class="w-full h-auto object-cover" src="{{ asset('img/Cubierta Pirelli1.webp') }}" alt="Cubierta Pirelli" />
                    <h2 class="mt-4 text-center text-[#161212] text-xl font-bold leading-tight">Cubierta Pirelli 205/55 R16 91w Run Flat P7 Cinturato</h2>
                </div>
                <div class="product flex flex-col items-center">
                    <img class="w-full h-auto object-cover" src="{{ asset('img/Aceite1.webp') }}" alt="Aceite para motor" />
                    <h2 class="mt-4 text-center text-[#161212] text-xl font-bold leading-tight">Aceite para motor Motul sintético 5W-40</h2>
                </div>
            </div>
        </div>
    
        <script>
        document.addEventListener('DOMContentLoaded', function() {
            const inputBuscar = document.getElementById('buscar');
            const productos = document.querySelectorAll('.product');
            const searchIcon = document.getElementById('searchIcon');
    
            function buscarProductos() {
                const textoBusqueda = inputBuscar.value.toLowerCase();
                productos.forEach(producto => {
                    const textoProducto = producto.textContent.toLowerCase();
                    if (textoProducto.includes(textoBusqueda) || textoBusqueda === '') {
                        producto.classList.remove('hidden');
                    } else {
                        producto.classList.add('hidden');
                    }
                });
            }
    
            inputBuscar.addEventListener('input', buscarProductos);
            inputBuscar.addEventListener('keyup', function(event) {
                if (event.key === 'Enter') {
                    buscarProductos();
                }
            });
    
            searchIcon.addEventListener('click', function() {
                inputBuscar.focus();
            });
        });
        </script>

            {% include 'partials/footer.html.twig' %}

    </body>
    </html>
</html>